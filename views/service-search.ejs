<!DOCTYPE html>
<html lang="sw">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tafuta Huduma</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }
        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body class="bg-gray-100">
    <header class="bg-gradient-to-r from-blue-600 to-purple-600 text-white p-4 shadow-md fixed w-full top-0 z-50">
        <nav class="container mx-auto flex justify-between items-center">
            <div class="text-2xl font-bold flex items-center">
                <i class="fas fa-hands-helping mr-2"></i>
                Service Connect
            </div>
            <div class="hidden md:flex space-x-6">
                <a href="main.html" class="hover:text-gray-300 transition-colors">Home</a>
                <a href="client-profile.html" class="hover:text-gray-300 transition-colors">Mteja</a>
              
            </div>
            <button class="md:hidden text-white text-2xl">☰</button>
        </nav>
    </header>

    <main class="mt-18">
        <!-- Search Section -->
        <section class="bg-gradient-to-br from-blue-500 to-purple-500 text-white py-16 px-4">
            <div class="container mx-auto max-w-4xl mt-10">
                <h1 class="text-4xl md:text-5xl font-bold text-center mb-4">Tafuta Mfanyakazi</h1>
                <p class="text-lg text-center mb-8">Chagua huduma kwa urahisi na upate mfanyakazi wa kuwania</p>
                
                <div class="bg-white rounded-xl shadow-2xl p-6">
                    <form id="searchForm" class="grid grid-cols-1 md:grid-cols-4 gap-4  bg-white text-black">
                        <div class="relative">
                            <select id="serviceType" class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-blue-500 ">
                                <option value="">Aina ya Huduma</option>
                                <option value="Usafi">Usafi wa Nyumba</option>
                                <option value="Bustani">Usimamizi wa Bustani</option>
                                <option value="Upishi">Upishi wa Kienyeji</option>
                                <option value="Ulinzi">Ulinzi na Usalama</option>
                                <option value="Ushonaji">Ushonaji na Urembo</option>
                            </select>
                        </div>
                        
                        <div class="relative">
                            <input type="text" id="location" 
                                   class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-blue-500" 
                                   placeholder="Eneo (Mfano: Dar es Salaam)">
                        </div>

                        <div class="relative">
                            <input type="number" id="minExperience" 
                                   class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-blue-500" 
                                   placeholder="Miaka ya Uzoefu (Chini)">
                        </div>

                        <div class="relative">
                            <input type="number" id="maxPrice" 
                                   class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-blue-500" 
                                   placeholder="Kiwango cha Bei (Tsh)">
                        </div>

                        <button type="submit" 
                                class="col-span-1 md:col-span-4 bg-gradient-to-r from-green-500 to-green-600 text-white p-3 rounded-lg hover:from-green-600 hover:to-green-700 transition-all flex items-center justify-center space-x-2">
                            <i class="fas fa-search"></i>
                            <span>Tafuta Sasa</span>
                        </button>
                    </form>
                </div>
            </div>
        </section>

        <!-- Results Section -->
        <section class="container mx-auto p-6">
            <div class="flex flex-col md:flex-row justify-between items-center mb-8">
                <h2 class="text-3xl font-semibold text-gray-800 mb-4 md:mb-0">Wafanyakazi Walio Patikana</h2>
                <div class="flex space-x-4">
                    <select id="sortBy" class="p-2 border rounded-lg">
                        <option value="rating">Panga kwa Ukadiriaji</option>
                        <option value="experience">Panga kwa Uzoefu</option>
                        <option value="price">Panga kwa Bei</option>
                    </select>
                </div>
            </div>

            <div id="results" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Results will be inserted here -->
            </div>

            <div id="loading" class="hidden text-center py-8">
                <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto"></div>
                <p class="mt-4 text-gray-600">Inatafuta wafanyakazi...</p>
            </div>
        </section>
    </main>

    <footer class="bg-gray-800 text-white text-center p-6 mt-12">
        <p class="mb-2">&copy; 2025 HudumaPro. Haki zote zimehifadhiwa.</p>
        <div class="flex justify-center space-x-4">
            <a href="#" class="hover:text-blue-400"><i class="fab fa-whatsapp"></i></a>
            <a href="#" class="hover:text-blue-400"><i class="fab fa-facebook"></i></a>
            <a href="#" class="hover:text-blue-400"><i class="fab fa-instagram"></i></a>
        </div>
    </footer>

    <script>
        // Mock Database (Replace with real API calls)
        const workers = [
            { 
                id: 1,
                name: "John Doe",
                service: "Usafi",
                location: "Dar es Salaam",
                experience: 2,
                rating: 4.5,
                price: 15000,
                image: "https://images.pexels.com/photos/774909/pexels-photo-774909.jpeg?auto=compress&cs=tinysrgb&w=600",
                bio: "Mtaalamu wa usafi wa nyumba kwa miaka 2. Najivunia kutoa huduma bora na kwa wakati."
            },
            { 
                id: 2,
                name: "Amina Mwajuma",
                service: "Bustani",
                location: "Dodoma",
                experience: 3,
                rating: 4.7,
                price: 20000,
                image: "https://images.pexels.com/photos/1181686/pexels-photo-1181686.jpeg?auto=compress&cs=tinysrgb&w=600",
                bio: "Mtaalamu wa bustani na ustawi wa mimea. Nimefanya miradi 50+ ya ubunifu wa bustani."
            },
            { 
                id: 3,
                name: "Hassan Juma",
                service: "Upishi",
                location: "Arusha",
                experience: 5,
                rating: 4.8,
                price: 25000,
                image: "https://images.pexels.com/photos/220453/pexels-photo-220453.jpeg?auto=compress&cs=tinysrgb&w=600",
                bio: "Mpishi mwenye uzoefu wa miaka 5 katika upishi wa vyakula vya kienyeji na kimataifa."
            }
        ];

        // Search Functionality
        document.getElementById("searchForm").addEventListener("submit", async (e) => {
            e.preventDefault();
            const resultsContainer = document.getElementById("results");
            const loading = document.getElementById("loading");
            
            // Show loading
            resultsContainer.innerHTML = '';
            loading.classList.remove('hidden');

            // Simulate API call
            await new Promise(resolve => setTimeout(resolve, 1000));

            // Get search parameters
            const serviceType = document.getElementById("serviceType").value;
            const location = document.getElementById("location").value.toLowerCase();
            const minExperience = parseInt(document.getElementById("minExperience").value) || 0;
            const maxPrice = parseInt(document.getElementById("maxPrice").value) || Infinity;
            const sortBy = document.getElementById("sortBy").value;

            // Filter workers
            let filteredWorkers = workers.filter(worker => {
                return (!serviceType || worker.service === serviceType) &&
                       (!location || worker.location.toLowerCase().includes(location)) &&
                       (worker.experience >= minExperience) &&
                       (worker.price <= maxPrice);
            });

            // Sort results
            filteredWorkers.sort((a, b) => b[sortBy] - a[sortBy]);

            // Display results
            loading.classList.add('hidden');
            resultsContainer.innerHTML = filteredWorkers.length > 0 
                ? filteredWorkers.map(worker => createWorkerCard(worker)).join('')
                : `<div class="col-span-full text-center py-8">
                        <i class="fas fa-search-minus text-4xl text-gray-400 mb-4"></i>
                        <p class="text-xl text-gray-600">Hakuna mfanyakazi aliyepatikana kwa vigezo ulivyoweka</p>
                   </div>`;

            // Add fade-in animation
            document.querySelectorAll('.worker-card').forEach(card => {
                card.classList.add('fade-in');
            });
        });

        // Create Worker Card Component
        function createWorkerCard(worker) {
            return `
                <div class="worker-card bg-white rounded-xl shadow-md overflow-hidden card-hover">
                    <img src="${worker.image}" alt="${worker.name}" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <div class="flex items-center justify-between mb-4">
                            <h3 class="text-xl font-bold">${worker.name}</h3>
                            <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm">${worker.service}</span>
                        </div>
                        <div class="space-y-2 text-gray-600">
                            <p><i class="fas fa-map-marker-alt mr-2"></i>${worker.location}</p>
                            <p><i class="fas fa-clock mr-2"></i>Uzoefu: ${worker.experience} Miaka</p>
                            <p><i class="fas fa-coins mr-2"></i>Bei kwa Siku: Tsh ${worker.price.toLocaleString()}</p>
                            <div class="flex items-center">
                                <div class="text-yellow-500 mr-2">${'⭐'.repeat(Math.floor(worker.rating))}</div>
                                <span class="text-sm">(${worker.rating})</span>
                            </div>
                        </div>
                        <p class="mt-4 text-gray-700">${worker.bio}</p>
                        <div class="mt-6 flex justify-between items-center">
                            <a href="booking.html?id=${worker.id}" 
                               class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                               <i class="fas fa-calendar-check mr-2"></i>Weka Ombi
                            </a>
                            <button class="text-blue-600 hover:text-blue-800">
                                <i class="fas fa-ellipsis-h"></i>
                            </button>
                        </div>
                    </div>
                </div>
            `;
        }

        // Initialize with all workers
        document.getElementById("results").innerHTML = workers.map(worker => createWorkerCard(worker)).join('');
    </script>
</body>
</html>

